# ⚠️ This is not the file served by GitHub Pages

This `prototype-mvp/index.html` is a **mirror copy** and is **not** what GitHub Pages deploys.

The live MVP prototype is served from:

```
docs/prototype-mvp/index.html
```

→ https://hmcts.github.io/hmcts-api-catalogue/prototype-mvp/

**If you're making changes to the MVP prototype, edit `docs/prototype-mvp/index.html`.**

This file has previously drifted out of sync after a change was committed here instead of to `docs/prototype-mvp/index.html` (see commit `2e0322b`, fixed in `1059459`). Avoid editing this copy unless you also apply the same change to `docs/prototype-mvp/index.html`.
